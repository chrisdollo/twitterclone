## installation and Setup

### Web Application (Node.js)
Navigate to the frontend folder

#### Install dependencies:
- npm install
```bash
echo "Hello, world!"
ls -l
```

#### Run the server:
- npm start